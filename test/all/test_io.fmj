public int main() {
    int[] a;
    int n;
    int i = 0;

    a = new int[10];
    n = getarray(a);

    while (i < n) {
        putint(a[i]);
        putch(32);
        i = i + 1;
    }
    putch(10);

    return 0;
}
