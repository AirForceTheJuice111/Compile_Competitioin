int main() {
  int x = getint();
  int min = getint();

  int a = x + 0;
  int b = 0 + x;
  int c = x * 1;
  int d = 1 * x;
  int e = x - x;
  int f = -1 * x;
  int g = x * -1;
  int h = x / 1;
  int same = 0;
  if (x == x) {
    same = 1;
  }

  putint(a + b + c + d + e + f + g + h + same);
  putch(10);
  putint(-1 * min);
  putch(10);
  putint(min / -1);
  putch(10);
  putint(min % -1);
  putch(10);
  return 0;
}
