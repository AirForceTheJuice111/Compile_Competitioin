public int main() {
    int i;
    int base;
    int step;
    i = getint();
    base = getint();
    while (i > 0) {
        step = base + 1;
        putint(3 * i + 2);
        putch(32);
        i = i - step;
    }
    putch(10);
    return i;
}
