public int main() {
    int i;
    int step;
    i = getint();
    step = getint();
    while (i > 0) {
        i = i - step;
        putint(6 * i + 1);
        putch(32);
    }
    putch(10);
    return i;
}
