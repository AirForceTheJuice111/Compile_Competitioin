public int main() {
    int i;
    int base;
    int step;
    i = getint();
    base = getint();
    while (i > 0) {
        step = base + 1;
        i = i - step;
        putint(3 * i + 2);
        putch(32);
    }
    putch(10);
    return i;
}
